import { RoundresultPipe } from './roundresult.pipe';

describe('RoundresultPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundresultPipe();
    expect(pipe).toBeTruthy();
  });
});
